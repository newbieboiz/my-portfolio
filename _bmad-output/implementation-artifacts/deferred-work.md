# Deferred Work

## Deferred from: code review of 1-1-project-initialization-tooling-setup (2026-05-14)

- Boilerplate `metadata` in `layout.tsx` (`title: "Create Next App"`, `description: "Generated by create next app"`) — update in a future layout/Hero story
- `globals.css` `body` overrides Geist with `font-family: Arial, Helvetica, sans-serif` — hardcoded font overrides `--font-geist-sans` CSS variable; owned by Story 1.2 or UX story
- Bare `wss:` in CSP `connect-src` is overly permissive — tighten to specific origins when deployment domain is known
- Custom production domain not in CSP `connect-src` — add `NEXT_PUBLIC_SITE_URL`-derived origin to connect-src when domain is set
- `NEXT_PUBLIC_SITE_URL` not validated at startup — add a startup guard when QR/OG/sitemap features are implemented
- `allowJs: true` without `checkJs: true` in `tsconfig.json` — scaffold default; consider enabling `checkJs` in a future TypeScript hygiene story
- No test infrastructure — out of scope for story 1-1; add test runner + baseline tests in a dedicated testing-setup story
